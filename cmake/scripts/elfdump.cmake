IF(NOT "$ENV{MW_INSTALL_PATH}" STREQUAL "")
    set(CMAKE_PREFIX_PATH  "$ENV{MW_INSTALL_PATH}/arc/bin/")
ENDIF()

EXECUTE_PROCESS(COMMAND ${CMAKE_PREFIX_PATH}elfdumpac -t -T ${IN} OUTPUT_FILE ${OUT})

Overview
^^^^^^^^

Unity is a framework for running unit tests directly on the target (DVK).

The unit tests are discovered and executed by a python script running on the host computer.

Link to github: `Unity on GitHub <https://github.com/ThrowTheSwitch/Unity>`_

.. note::

    Unity is not related to Unity 3D (the 3D engine).
